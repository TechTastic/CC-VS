Functions in the Ship API:
ship.getId()
ship.getMass()
ship.getMomentOfInertiaTensorToSave()
ship.getMomentOfInertiaTensor()
ship.getSlug()
ship.getAngularVelocity()
ship.getQuaternion()
ship.getScale()
ship.getShipyardPosition()
ship.getSize()
ship.getVelocity()
ship.getWorldspacePosition()
ship.transformPositionToWorld( number, number, number )
ship.transformPositionToWorld( vector )
ship.isStatic()
ship.setSlug( string )
ship.getTransformationMatrix()
ship.getJoints()
ship.pullPhysicsTicks()

Note: Methods past this point require a Command Computer by default!

ship.applyWorldForce( vector, vector | nil )
ship.applyWorldTorque( vector )
ship.applyModelForce( vector, vector | nil )
ship.applyModelTorque( vector )
ship.applyWorldForceToModelPos( vector, vector )
ship.applyBodyForce( vector, vector | nil )
ship.applyBodyTorque( vector )
ship.applyWorldForceToBodyPos( vector, vector )
ship.setStatic( boolean )
ship.setScale( number )
ship.teleport( table )